require 'spec_helper'

describe Dotenv::Compliment do
  it 'has a version number' do
    expect(Dotenv::Compliment::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
